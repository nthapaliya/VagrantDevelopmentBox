#!/usr/bin/env bash

# export PATH="$HOME/.rbenv/bin:$PATH"
# eval "$(rbenv init -)"


# echo "Attempting to compile ruby"
# echo "This may take a while ..."

# this makes .ruby-version untracked in our vm
# nobody makes changes to this file so it doesnt matter
# cd ~/OSS/huddle
# git update-index --assume-unchanged .ruby-version
# echo '2.2.2' > .ruby-version
# rbenv install &>/dev/null
# rbenv rehash

# echo "Installing bundler"
# gem install bundler --no-ri --no-rdoc >/dev/null

# echo "Running bundle install for the first time"
# echo "This may take a while ..."
# bundle &>/dev/null
# rbenv rehash
